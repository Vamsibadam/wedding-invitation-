OPTIONAL ASSETS

1. Add public/music.mp3 to enable the floating music button.
2. Replace the sample Unsplash image URLs in src/App.tsx with your own:
   - bride portrait
   - groom portrait
   - event images
   - gallery images

The page is intentionally single-page and mobile-first. It uses original CSS/SVG-like decorative shapes and animation patterns inspired by the supplied reference recording rather than copying the reference site's artwork.